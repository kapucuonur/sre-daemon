[
  "^docker compose (up|restart)( -d)?( --build)?$",
  "^docker compose (up|restart)( -d)?( --build)? [a-zA-Z0-9_-]+$",
  "^docker compose (up|restart)( -d)?( --build)?( [a-zA-Z0-9_-]+)?$",
  "^docker compose down$"
]